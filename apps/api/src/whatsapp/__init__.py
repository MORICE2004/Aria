"""WhatsApp interface package.

Phase 8 scope: OBSERVE MODE ONLY. ARIA ingests messages, learns from them,
and classifies context. She cannot send — not because a prompt tells her not
to, but because no send path exists outside the Action Gateway, and the
autonomy resolver refuses to grant send capability in observe mode.
"""
